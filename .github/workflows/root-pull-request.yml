name: Root - Pull Request
on:
  pull_request:
    branches:
      - daily

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Post the knip results
        uses: codex-/knip-reporter@v2
