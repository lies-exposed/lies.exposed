{"_schema":"playbook","_version":1,"_description":"Reproducible bugs and footguns in lies.exposed, with diagnosis steps and fixes","_fields":{"id":"string — {type}_{YYYYMMDD}_{HHMMSS}_{4hex}","title":"string","symptom":"string — what the developer observes","root_cause":"string — why it happens","fix":"string — what to do","area":"string — which part of the codebase","status":"string — active|archived"}}
{"id":"playbook_20260101_000000_0001","title":"TypeORM serializes Option wrapper instead of null","symptom":"DB column stores {_tag:'None'} or {_tag:'Some',value:...} as JSON instead of null or the actual value","root_cause":"OptionFromNullishToNull returns effect/Option, not fp-ts/Option. Calling fp-ts O.toNullable() on it fails silently and TypeORM serializes the wrapper object.","fix":"Use O.getOrNull() from 'effect/Option' instead of O.toNullable() from 'fp-ts/Option'","area":"packages/@liexp/backend, any TypeORM entity using OptionFromNullishToNull","status":"active"}
{"id":"playbook_20260101_000001_0002","title":"OpenAI structured output fails with optional schema fields","symptom":"OpenAI API returns 400 or generates empty/malformed JSON when structured output schema has optional properties","root_cause":"OpenAI structured output rejects JSON schemas with optional (non-required) fields. Schema.optional() produces optional JSON schema properties.","fix":"Use Schema.NullOr(T) instead of Schema.optional(T). Ensure ALL properties appear in the required array.","area":"services/ai-bot, services/agent — any OpenAI structured output schema","status":"active"}
{"id":"playbook_20260101_000002_0003","title":"Agent service loses LocalAI connection after container restart","symptom":"Agent returns connection errors after LocalAI container was restarted, even though LocalAI is healthy","root_cause":"Docker/K8s DNS caches the old IP of the LocalAI container. Agent resolved it at startup and holds the stale address.","fix":"Restart the agent service after restarting LocalAI to force DNS re-resolution.","area":"services/agent","status":"active"}
{"id":"playbook_20260101_000003_0004","title":"Qwen3 model returns empty content (reasoning mode)","symptom":"LangChain chat completions return empty string content from Qwen3 models","root_cause":"Qwen3 defaults to thinking mode, putting output in delta.reasoning instead of delta.content. LangChain's @langchain/openai ignores reasoning field.","fix":"In LocalAI model yaml: set reasoning.disable: true AND add template prefill '<think>\\n</think>' after '<|im_start|>assistant'. Both settings required.","area":"services/localai (LocalAI model config yaml)","status":"active"}
{"id":"playbook_20260101_000004_0005","title":"Storybook type errors after adding new @liexp/ui files","symptom":"Storybook type-check fails with 'cannot find module' for newly added UI components","root_cause":"Storybook imports from @liexp/ui/lib/... (build output). New source files don't exist in lib/ until rebuilt.","fix":"Run 'pnpm --filter @liexp/ui run build' before type-checking storybook after adding new UI files.","area":"services/storybook, packages/@liexp/ui","status":"active"}
{"id":"playbook_20260713_173300_a1b2","title":"agent CLI event create silently returns list data instead of created event (http->https redirect demotes POST to GET)","symptom":"liexp_cli 'event <type> create' fails with DecodeError: API client decode error, actual data is an array of unrelated pre-existing events (each with score:1) instead of the single created event","root_cause":"services/agent/.env or .env.local had API_BASE_URL=http://api.liexp.dev/v1 (or test/AgentEvalTest.ts hardcoded the same). Dev nginx 301-redirects http->https. axios/follow-redirects demotes POST to GET on a 301 by default, dropping the body, so POST /events silently became GET /events and hit ListEventRoute instead of CreateEventRoute.","fix":"Always set API_BASE_URL (and MCP_URL) to https://api.liexp.dev/v1 directly in services/agent/.env, .env.local, and test/AgentEvalTest.ts — never rely on the dev nginx redirect for non-GET requests.","area":"services/agent (.env, .env.local, test/AgentEvalTest.ts), dev nginx reverse proxy","status":"active"}
{"id":"playbook_20260713_173300_c3d4","title":"Service-client API tokens 404 on authenticated write routes (id vs userId type confusion)","symptom":"liexp_cli mutations (event create/edit etc.) fail with HTTP 500 wrapping AxiosError 404: 'Could not find any entity of type UserEntity matching: {id: <uuid>}', while read-only routes (event list) work fine with the same token","root_cause":"scripts/generate-tokens.sh (services/api/src/bin/generate-service-token.ts) signs a ServiceClient-shaped JWT with a fresh random 'id' (client id) and a separate 'userId' (real user reference) — and never touches the DB, so neither id is ever persisted. Write routes like createEvent.controller.ts only call RequestDecoder.decodeUserFromRequest (AuthUser schema: id/email/permissions/iat/exp), which structurally accepts the ServiceClient payload too (effect Schema.Struct ignores the extra userId key) and reads the client's random 'id' as if it were the real user id. UserRepository.findOneOrFail then 404s because no user row has that id. Breaks whenever the dev DB is reseeded (old matching user row disappears) or a fresh token is generated (new random id, no matching user row at all).","fix":"Short-term: after running scripts/generate-tokens.sh, manually INSERT a user row in the dev DB whose id equals the token's printed 'ID:' (client id) field, with permissions matching, then use that token as API_TOKEN. Proper fix (deferred): write routes should use RequestDecoder.decodeUserOrServiceClient and look up UserRepository by client.userId, not client.id, so service-client tokens don't need a fake matching user row at all.","area":"services/api (routes/events/createEvent.controller.ts, express/decoders/request.decoder.ts), scripts/generate-tokens.sh, services/agent/.env.local API_TOKEN","status":"active"}
