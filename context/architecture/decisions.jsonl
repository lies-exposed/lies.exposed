{"_schema":"adr","_version":1,"_description":"Architecture Decision Records for lies.exposed","_fields":{"id":"string — {type}_{YYYYMMDD}_{HHMMSS}_{4hex}","title":"string","decision":"string — what was decided","rationale":"string — why","status":"string — active|superseded|archived","supersedes":"string|null — id of superseded ADR"}}
{"id":"adr_20260101_000000_0001","title":"fp-ts + Effect dual library strategy","decision":"Use fp-ts (pipe, TaskEither, ReaderTaskEither) for all business logic pipelines. Use Effect/Schema only for HTTP schema definitions (@effect/schema). Do not mix in same module.","rationale":"fp-ts was the original choice; Effect was introduced for schema validation. Mixing both fully in pipelines creates confusion. Clear boundary: Effect for schema, fp-ts for logic.","status":"active","supersedes":null}
{"id":"adr_20260101_000001_0002","title":"OpenAI structured output: all fields required","decision":"All Effect/Schema objects used in OpenAI structured output must have every property required. Use Schema.NullOr() instead of Schema.optional().","rationale":"OpenAI structured output rejects schemas with optional fields. Schema.optional() maps to optional JSON schema which causes API errors.","status":"active","supersedes":null}
{"id":"adr_20260101_000002_0003","title":"OptionFromNullishToNull returns effect/Option not fp-ts/Option","decision":"Use O.getOrNull() from effect/Option, never O.toNullable() from fp-ts/Option, when unwrapping OptionFromNullishToNull values.","rationale":"OptionFromNullishToNull returns effect/Option. Calling fp-ts O.toNullable() on it causes TypeORM to serialize the Option wrapper object instead of the raw value, silently corrupting data.","status":"active","supersedes":null}
{"id":"adr_20260910_203210_8e19","title":"Story editor autosave via localStorage draft cache","decision":"Draft recovery for react-admin forms is a localStorage cache: useFormAutosave hook + FormAutosave banner in packages/@liexp/ui/src/components/admin/common/. Debounced snapshot of react-hook-form values, key liexp:autosave:<resource>:<recordId|new>, cleared on successful submit or explicit discard, stale drafts (older than record.updatedAt) dropped on mount. includePaths option narrows the snapshot to specific fields. Wired into Story create/edit with includePaths=[\"body2\"] so only the BlockNote article body is cached (issue #813).","rationale":"Issue #813 offered localStorage or periodic API writes. localStorage adds zero server load, needs no endpoint, survives crash/accidental-nav, never persists half-valid records. Scope is the BlockNote body only \u2014 the field that is expensive to retype; @blocknote/core has no built-in persistence and react-admin v5 <AutoSave> is enterprise-only.","status":"active","supersedes":null}
