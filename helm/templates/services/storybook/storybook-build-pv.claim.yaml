apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storybook-build-pv-claim
  namespace: {{ .Release.Namespace }}
{{ tuple . "storybook-build-pv-claim" | include "storybook.labels" | indent 2 }}
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
