apiVersion: apps/v1
kind: Deployment
metadata:
  name: telegram-bot-api
  namespace: {{ .Release.Namespace }}
{{ tuple . "telegram-bot-api" | include "tg.labels" | indent 2 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      lies.exposed/name: telegram-bot-api
      app.kubernetes.io/instance: {{ .Release.Name | quote }}
  strategy:
    type: Recreate
  template:
    metadata:
{{ tuple . "telegram-bot-api" | include "tg.labels" | indent 6 }}
    spec:
      containers:
        - name: telegram-bot-api
          envFrom:
            - configMapRef:
                name: telegram-bot-api-env
          image: aiogram/telegram-bot-api:latest
          ports:
            - containerPort: 8081
              protocol: TCP
          stdin: true
          volumeMounts:
            - name: telegram-bot-api-data
              mountPath: /var/lib/telegram-bot-api
          resources:
            limits:
              memory: 512Mi
              cpu: 300m
            requests:
              memory: 256Mi
              cpu: 100m
      restartPolicy: Always
      volumes:
        - name: telegram-bot-api-data
          persistentVolumeClaim:
            claimName: telegram-bot-api-data-pv-claim
